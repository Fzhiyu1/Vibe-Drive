# 阶段 1: 项目初始化

## 状态

🟢 已完成

## 目标

搭建项目基础骨架，配置开发环境，引入核心依赖。

## 前置依赖

无

## 任务清单

### 后端项目初始化

- [x] 使用 Spring Initializr 创建 Spring Boot 3.x 项目
- [x] 配置 Maven/Gradle 依赖管理
- [x] 引入 LangChain4j 依赖
- [x] 引入 Spring Web、Lombok 等基础依赖
- [x] 配置 application.yml（端口、日志等）
- [x] 创建基础包结构（controller、service、tool、model）

### 前端项目初始化

- [x] 使用 Vite 创建 React + TypeScript 项目
- [x] 引入 Three.js 依赖
- [x] 配置基础项目结构
- [x] 配置代理（连接后端 API）

### 环境配置

- [x] 创建 .env.example 文件（LLM API Key 模板）
- [x] 配置 .gitignore（排除敏感文件）
- [x] 编写项目启动说明

## 相关文件

```
vibe-drive-backend/
├── pom.xml (或 build.gradle)
├── src/main/java/com/vibe/
│   ├── VibeDriveApplication.java
│   ├── controller/
│   ├── service/
│   ├── tool/
│   └── model/
└── src/main/resources/
    └── application.yml

vibe-drive-frontend/
├── package.json
├── vite.config.ts
└── src/
    ├── App.tsx
    └── main.tsx
```

## 完成标准

- [x] 后端项目可正常启动（`mvn spring-boot:run`）
- [x] 前端项目可正常启动（`npm run dev`）
- [x] LangChain4j 依赖引入成功
- [ ] 代码已提交到 Git

## 启动说明

### 后端启动
```bash
cd vibe-drive-backend
mvn spring-boot:run
```
访问: http://localhost:8080

### 前端启动
```bash
cd vibe-drive-frontend
npm install
npm run dev
```
访问: http://localhost:5173

## 问题与笔记

（开发过程中遇到的问题和解决方案记录在这里）
